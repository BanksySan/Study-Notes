config:
  target: http://localhost:3000
  phases:
    - duration: 1
      arrivalRate: 1
  plugins:
    demo: {
            message1: "Message 1",
            message2: "Message 2",
    }
scenarios:
  - flow:
      - get:
          url: "/echo/foobar"
